name: IDIS Dashboard Update

on:
  schedule:
    - cron: '0 0 * * *'    # 매일 GMT 00:00 = KST 09:00
  workflow_dispatch:
  push:
    paths:
      - '*.xls'
      - '*.xlsx'
      - 'data/**'

jobs:
  update-dashboard:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: 저장소 체크아웃
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Python 설정
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 패키지 설치
        run: pip install pandas openpyxl lxml requests boto3

      - name: 폴더 생성
        run: mkdir -p output

      - name: 파이프라인 실행
        env:
          AWS_ACCESS_KEY_ID:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        run: python run_pipeline.py --skip-download --data-dir . --upload-s3

      - name: 결과 확인
        if: always()
        run: ls -la output/ || echo "없음"

      - name: 아티팩트 저장
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: dashboard-data-${{ github.run_number }}
          path: output/
          retention-days: 7
